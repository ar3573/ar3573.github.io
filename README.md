# AR3573 Quarto Site

Project-focused personal site for assignments and projects.

## Edit Work Items

Update `assets/work.js`. Each record drives the cards, tables, counts, filters, and stack matrix.

## Local Preview

```bash
quarto preview
```

## Render

```bash
quarto render
```

GitHub Actions publishes `_site` to GitHub Pages on every push to `main`.
